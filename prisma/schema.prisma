generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String?
  access_token      String?
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String?
  session_state     String?
  user              User    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

model State {
  id        Int     @id
  name      String  @unique
  sigla     String  @unique
  cities    City[]
}

model City {
  id        Int     @id
  name      String
  slug      String  @unique // Slug único: nome-da-cidade-sigla-estado
  stateId   Int
  state     State   @relation(fields: [stateId], references: [id])
  users     User[]  // Usuários que têm esta cidade como localização
  favoriteUsers User[] @relation("UserFavoriteCities") // Usuários que têm esta cidade como favorita
  medias    Media[] // Mídias desta cidade

  @@index([stateId])
}

model User {
  id            String    @id @default(cuid())
  name          String?
  displayName   String?   // Nome personalizado para exibição no site
  email         String    @unique
  emailVerified DateTime?
  image         String?
  stateId       Int?      // ID do estado (ex: 11 para Rondônia)
  cityId        Int?      // ID da cidade (ex: 1100015)
  city          City?     @relation(fields: [cityId], references: [id])
  favoriteCities City[]   @relation("UserFavoriteCities") // Cidades favoritas do usuário
  accounts      Account[]
  sessions      Session[]
  medias        Media[]
  knowledge     MediaKnowledge[] // Relacionamento com conhecimentos
}

model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}

model Media {
  id        String   @id @default(cuid())
  permalink String   @unique // Link permanente único baseado em timestamp
  publicId  String?  @unique // ID único do Cloudinary (opcional para texto)
  url       String?  // URL pública do arquivo (opcional para texto)
  text      String?  @db.Text // Texto da mensagem (opcional) - usando TEXT para textos longos
  type      String   // "image", "video", "audio", "text"
  categories String? // JSON array com categorias: ["local", "pessoa", "historia"]
  userId    String   // Relaciona com o usuário
  stateId   Int      // ID do estado
  cityId    Int      // ID da cidade
  city      City     @relation(fields: [cityId], references: [id])
  createdAt DateTime @default(now())
  score     Int      @default(0) // Pontuação baseada em comentários e conhecimentos
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  parentId  String?  // Novo campo: ID da mídia pai (para comentários)
  parent    Media?   @relation("MediaReplies", fields: [parentId], references: [id], onDelete: Cascade)
  replies   Media[]  @relation("MediaReplies") // Relacionamento para respostas
  knowledge MediaKnowledge[] // Relacionamento com conhecimentos

  @@index([userId])
  @@index([stateId, cityId])
  @@index([parentId]) // Novo índice para buscas eficientes
  @@index([score]) // Novo índice para ordenação por pontuação
  @@index([permalink]) // Novo índice para links permanentes
}

model MediaKnowledge {
  id       String @id @default(cuid())
  userId   String
  mediaId  String
  user     User   @relation(fields: [userId], references: [id], onDelete: Cascade)
  media    Media  @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  createdAt DateTime @default(now())

  @@unique([userId, mediaId]) // Um usuário só pode conhecer uma mídia uma vez
  @@index([userId])
  @@index([mediaId])
}